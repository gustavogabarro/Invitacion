<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>¡Te Invito a mi Fiesta! | [Nombre del Evento]</title>
    <style>
        /* Variables y Reseteo */
        :root {
            --color-primary: #ff4500; /* Naranja Brillante */
            --color-secondary: #008080; /* Teal */
            --color-text: #333;
            --color-bg: #f5f5f5;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Arial', sans-serif;
            line-height: 1.6;
            color: var(--color-text);
            background-color: var(--color-bg);
        }

        .content {
            max-width: 900px;
            margin: 0 auto;
            padding: 20px;
            text-align: center;
        }

        /* 1. Header con Imagen Responsiva */
        .header {
            width: 100%;
            /* **IMPORTANTE:** Reemplaza 'images/header-mobile.jpg' por tu imagen móvil */
            background-image: url('images/header-mobile.jpg'); 
            background-size: cover;
            background-position: center;
            height: 60vh; /* Alto para móvil (vertical) */
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
        }

        /* Media Query para Web (Escritorio/Tablet grande) */
        @media (min-width: 768px) {
            .header {
                /* **IMPORTANTE:** Reemplaza 'images/header-web.jpg' por tu imagen web */
                background-image: url('images/header-web.jpg'); 
                height: 500px; /* Alto para web (horizontal) */
            }
        }

        /* Secciones */
        section {
            padding: 20px 0;
        }
        
        /* 2. Contador Regresivo */
        .countdown-section {
            padding: 40px 20px;
            background-color: #fff;
            border-radius: 8px;
            margin: 20px 0;
            box-shadow: 0 6px 15px rgba(0,0,0,0.1);
        }

        .countdown {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 25px;
        }

        .countdown-item {
            display: flex;
            flex-direction: column;
            align-items: center;
            background-color: var(--color-bg);
            padding: 15px;
            border-radius: 8px;
            min-width: 70px;
        }

        .countdown-item span {
            font-size: 2.5em;
            font-weight: bold;
            color: var(--color-primary);
        }

        .countdown-item label {
            font-size: 0.9em;
            text-transform: uppercase;
            color: #666;
            margin-top: 5px;
        }

        /* 3. Botón de Confirmación (RSVP) */
        .rsvp-section {
            margin: 30px 0;
        }

        .button-rsvp {
            display: inline-block;
            padding: 18px 35px;
            background-color: var(--color-primary);
            color: white;
            text-decoration: none;
            font-size: 1.4em;
            font-weight: bold;
            border-radius: 50px; 
            transition: background-color 0.3s ease, transform 0.2s ease;
            box-shadow: 0 6px 12px rgba(255, 69, 0, 0.4);
            letter-spacing: 1px;
        }

        .button-rsvp:hover {
            background-color: #e63900; 
            transform: translateY(-3px);
        }

        /* 4. Ubicación */
        .location-section {
            padding: 40px 20px;
            background-color: #fff;
            margin-top: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0,0,0,0.05);
        }

        .button-map {
            display: inline-block;
            margin-top: 15px;
            padding: 10px 20px;
            background-color: var(--color-secondary);
            color: white;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s;
        }

        .button-map:hover {
            background-color: #006666;
        }

        /* 5. Logos Sponsors */
        .sponsors-section {
            border-top: 1px solid #ddd;
            padding: 30px 0;
            margin-top: 40px;
        }

        .sponsors-logos {
            display: flex;
            justify-content: center;
            gap: 40px;
            margin-top: 20px;
            flex-wrap: wrap; /* Para que se vean bien en móvil */
        }

        .sponsor-logo {
            max-width: 150px;
            height: auto;
            opacity: 0.8;
            transition: opacity 0.3s;
            filter: grayscale(10%);
        }

        .sponsor-logo:hover {
            opacity: 1;
            filter: grayscale(0%);
        }

        .copyright {
            margin-top: 30px;
            font-size: 0.8em;
            color: #999;
        }
    </style>
</head>
<body>

    <header class="header"></header>

    <main class="content">

        <section class="info">
            <h1>¡Te Esperamos en la Fiesta del Año!</h1>
            <p>Prepárate para una noche inolvidable de música y diversión el 5 de diciembre de 2025.</p>
        </section>

        <section class="countdown-section">
            <h2>⏳ Faltan:</h2>
            <div id="countdown" class="countdown">
                <div class="countdown-item">
                    <span id="days">00</span>
                    <label>Días</label>
                </div>
                <div class="countdown-item">
                    <span id="hours">00</span>
                    <label>Horas</label>
                </div>
                <div class="countdown-item">
                    <span id="minutes">00</span>
                    <label>Minutos</label>
                </div>
                <div class="countdown-item">
                    <span id="seconds">00</span>
                    <label>Segundos</label>
                </div>
            </div>
        </section>

        <section class="rsvp-section">
            <a href="[REEMPLAZAR_CON_ENLACE_A_TU_FORMULARIO]" target="_blank" class="button-rsvp">
                ¡No me la pierdo!
            </a>
            <p style="margin-top: 10px;">¡Confirma tu asistencia lo antes posible!</p>
        </section>

        <section class="location-section">
            <h2>📍 ¿Dónde es la celebración?</h2>
            <p style="font-size: 1.1em; font-weight: bold; margin: 10px 0;">Salón El Gran Evento - Av. Principal Ficticia 4567</p>
            <p>Habrá servicio de valet parking disponible.</p>
            <a href="[REEMPLAZAR_CON_ENLACE_A_GOOGLE_MAPS]" target="_blank" class="button-map">
                Abrir Ubicación en el Mapa
            </a>
        </section>

        <footer class="sponsors-section">
            <h2>Gracias a nuestros patrocinadores:</h2>
            <div class="sponsors-logos">
                <img src="images/logo-sponsor-1.png" alt="Sponsor Logo 1" class="sponsor-logo">
                <img src="images/logo-sponsor-2.png" alt="Sponsor Logo 2" class="sponsor-logo">
            </div>
            <p class="copyright">Invitación diseñada para el evento del 5 de diciembre de 2025.</p>
        </footer>

    </main>

    <script>
        // Fecha del evento: 5 de diciembre de 2025 a las 9 pm (21:00:00)
        // La fecha se usa como string para asegurar la compatibilidad: "Month Day, Year HH:MM:SS"
        const eventDate = new Date("December 5, 2025 21:00:00").getTime();

        // Obtener elementos del DOM
        const daysEl = document.getElementById('days');
        const hoursEl = document.getElementById('hours');
        const minutesEl = document.getElementById('minutes');
        const secondsEl = document.getElementById('seconds');

        function updateCountdown() {
            const now = new Date().getTime();
            const distance = eventDate - now;

            // Cálculos de tiempo
            const days = Math.floor(distance / (1000 * 60 * 60 * 24));
            const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
            const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
            const seconds = Math.floor((distance % (1000 * 60)) / 1000);

            // Formatear para que siempre tenga dos dígitos
            const formatTime = (time) => String(time).padStart(2, '0');

            // Actualizar el DOM y detener si ya pasó la fecha
            if (distance > 0) {
                daysEl.innerHTML = formatTime(days);
                hoursEl.innerHTML = formatTime(hours);
                minutesEl.innerHTML = formatTime(minutes);
                secondsEl.innerHTML = formatTime(seconds);
            } else {
                clearInterval(countdownInterval);
                document.getElementById("countdown").innerHTML = "<h2>🎉 ¡LA FIESTA YA EMPEZÓ! ¡Te esperamos!</h2>";
                // Opcional: Ocultar o cambiar el botón de RSVP
                // document.querySelector('.rsvp-section').style.display = 'none';
            }
        }

        // Actualizar el contador inmediatamente y luego cada segundo
        updateCountdown();
        const countdownInterval = setInterval(updateCountdown, 1000);
    </script>
</body>
</html>